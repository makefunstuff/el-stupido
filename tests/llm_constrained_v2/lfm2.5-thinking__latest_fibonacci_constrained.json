{
  "functions": [
    {
      "name": "fibonacci",
      "params": "n",
      "body": "n < 2 ? n : fib(n-1) + fib(n-2)"
    },
    {
      "name": "Ternary",
      "params": "cond",
      "body": "print(f(a..=b))"
    },
    {
      "name": "ForLoop",
      "params": "i",
      "body": "for i := 1..=n { print(fib(i)) }"
    }
  ],
  "main_body": "Define fibonacci recursively, print first 20 fibonacci numbers."
}