fibonacci(n) = n < 2 ? n : fib(n-1) + fib(n-2)
Ternary(cond) = print(f(a..=b))
fn ForLoop(i) {
  for i := 1..=n { print(fib(i)) }
}

fn main() {
  Define fibonacci recursively, print first 20 fibonacci numbers.
}
