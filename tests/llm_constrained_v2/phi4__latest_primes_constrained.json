{
  "functions": [
    {
      "name": "main",
      "params": "",
      "body": "for i := 2..=50 { if is_prime(i) { print(i) } }"
    },
    {
      "name": "is_prime",
      "params": "n",
      "body": "if n <= 1 { return false } else { for d := 2..=sqrt(n) { if n % d == 0 { return false } } return true }"
    }
  ],
  "main_body": "main()"
}