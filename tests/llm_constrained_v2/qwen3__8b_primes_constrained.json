{
  "functions": [
    {
      "name": "main",
      "params": "none",
      "body": "for i := 2..=50 { if is_prime(i) { print(i) } }"
    },
    {
      "name": "is_prime",
      "params": "n",
      "body": "if n == 2 { return true } if n % 2 == 0 { return false } for i := 3..=sqrt(n) step 2 { if n % i == 0 { return false } } return true"
    }
  ],
  "main_body": "for i := 2..=50 { if is_prime(i) { print(i) } }"
}