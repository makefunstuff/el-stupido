{
  "functions": [
    {
      "name": "isPrime",
      "params": "[2..n]",
      "body": "(i, n) => (for i <- a..b do if mod(n % d = 0 for all integers in range[d:a]) then return false else true;"
    },
    {
      "name": "main_programm",
      "params": "",
      "body": "(none) => (for i <- a..b do if isPrime(i,50)) println('prime number') end for loop."
    }
  ],
  "main_body": "println((isPrime[2 to n]))"
}