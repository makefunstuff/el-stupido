fn isPrime([2..n]) {
  (i, n) => (for i <- a..b do if mod(n % d = 0 for all integers in range[d:a]) then return false else true
}
fn main_programm() {
  (none) => (for i <- a..b do if isPrime(i,50)) println('prime number') end for loop.
}

fn main() {
  println((isPrime[2 to n]))
}
