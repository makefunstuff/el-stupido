{
  "functions": [
    {
      "name": "sum",
      "params": "f(range)",
      "body": "acc := 0; for i := 1..=n { acc += f(i) }"
    },
    {
      "name": "print",
      "params": "expr",
      "body": "println(expr)"
    }
  ],
  "main_body": "sum(f(range)) := 338350; print(sum(f(range)))"
}