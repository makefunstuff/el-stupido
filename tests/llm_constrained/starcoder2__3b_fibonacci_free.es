The language is a "toy" language, with no type system or garbage collection. It's just a bunch of functions and variables. The interpreter is written in C.

The compiler is written in Python 3.8.

The test suite is written in Rust.

The tests are run using Docker.