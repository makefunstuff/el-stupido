{
  "functions": [
    {
      "name": "main",
      "params": "none",
      "body": "for i := 2..=50 { if is_prime(i) { print(i) } }"
    },
    {
      "name": "is_prime",
      "params": "n",
      "body": "for j := 2..=n-1 { if n % j == 0 { break } } return n > 1"
    }
  ],
  "main_body": "for i := 2..=50 { if is_prime(i) { print(i) } }"
}