{
  "functions": [
    {
      "name": "fib",
      "params": "n",
      "body": "n < 2 ? n : fib(n-1) + fib(n-2)"
    },
    {
      "name": "print",
      "params": "f",
      "body": "for i := 1..=20 { print(fib(i)) }"
    }
  ],
  "main_body": "print(fib(20))"
}