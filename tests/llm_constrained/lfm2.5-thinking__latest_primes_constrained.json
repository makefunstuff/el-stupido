{
  "functions": [
    {
      "name": "isPrime",
      "params": "x",
      "body": "true if x is prime, else false"
    }
  ],
  "main_body": "for (i = 2; i <= sqrt(50); i++) { if (isPrime(i)) { print(i); } }"
}