name(args) = expr (one-liner fn, types inferred as i32)
Built-ins: product(1..=n) sum(1..n) count(a..b)
print(expr) auto-formats. for i := 1..=n { }. if/else/while/return.
fib(n) = n < 2 ? n : fib(n-1) + fib(n-2)
fact(n) = product(1..=n)
fn main() { for i := 1..=10 { print(fib(i)) } }

Write: factorial using sum(), print factorials 1 through 12.
Output ONLY code, no explanation, no markdown.
name(args) = expr (one-liner fn, types inferred as i32)
Built-ins: product(1..=n) sum(1..n) count(a..b)
print(expr) auto-formats. for i := 1..=n { }. if/else/while/return.
fib(n) = n < 2 ? n : fib(n-1) + fib(n-2)
fact(n) = sum(1..=n)
fn main() { for i := 1..=10 { print(fib(i)) } }

Write: factorial using count(), print factorials 1 through 12.
Output ONLY code, no explanation, no markdown.
name(args) = expr (one-liner fn, types inferred as i32)
Built-ins: product(1..=n) sum(1..n) count(a..b)
print(expr) auto-formats. for i := 1..=n { }. if/else/while/return.
fib(n) = n < 2 ? n : fib(n-1) + fib(n-2)
fact(n) = count(1..=n)
fn main() { for i := 1..=10 { print(fib(i)) } }

Write: factorial using sum(), print factorials 1 through 12.
Output ONLY code, no explanation, no markdown.
name(args) = expr (one-liner fn, types inferred as i32)
Built-ins: product(1..=n) sum(1..n) count(a..b)
print(expr) auto-formats. for i := 1..=n { }. if/else/while/return.
fib(n) = n < 2 ? n : fib(n-1) + fib(n-2)
fact(n) = sum(1..=n)
fn main() { for i := 1..=10 { print(fib(i)) } }

Write: factorial using sum(), print factorials 1 through 12.
Output ONLY code, no explanation, no markdown.
name(args) = expr (one-liner fn, types inferred as i32)
Built-ins: product(1..=n) sum(1..n) count(a..b)
print(expr) auto-formats. for i := 1..=n { }. if/else/while/return.
fib(n) = n < 2 ? n : fib(n-1) + fib(n-2)
fact(n) = sum(1..=n)
fn main() { for i := 1..=10 { print(fib(i)) } }

Write: factorial using sum(), print factorials 1 through 12.
Output ONLY code, no explanation, no markdown.
name(args) = expr (one-liner fn, types inferred as i32)
Built-ins: product(1..=n) sum(1..n) count(a..b)
print(expr) auto-formats. for i := 1..=n { }. if/else/while/return.
fib(n) = n < 2 ? n : fib(n-1) + fib(n-2)
fact(n) = sum(1..=n)
fn main() { for i := 1..=10 { print(fib(i)) } }

Write: factorial using sum(), print factorials 1 through 12.
Output ONLY code, no explanation, no markdown.
name(args) = expr (one-liner fn, types inferred as i32)
Built-ins: product(1..=n) sum(1..n) count(a..b)
print(expr) auto-formats. for i := 1