Loading model... |-\|/- 
▄▄ ▄▄
██ ██
██ ██  ▀▀█▄ ███▄███▄  ▀▀█▄    ▄████ ████▄ ████▄
██ ██ ▄█▀██ ██ ██ ██ ▄█▀██    ██    ██ ██ ██ ██
██ ██ ▀█▄██ ██ ██ ██ ▀█▄██ ██ ▀████ ████▀ ████▀
                                    ██    ██
                                    ▀▀    ▀▀
build      : b1-9051663
model      : sha256-2c0d857fd309d0129447bd82ec1b8ca467a4b683391160855540f49b278bda3e
modalities : text
available commands:
  /exit or Ctrl+C     stop or exit
  /regen              regenerate the last response
  /clear              clear the chat history
  /read               add a text file
> el-stupido: compiled lang. Built-ins: product(1..=n) sum(1..n).
print(expr) prints value. for i := 1..=n { }. name(args) = expr defines function.
Example: fib(n) = n < 2 ? n : fib(n-1) + fib(n-2)
Task: factorial function using product(1..=n), print factorials 1..12.
| Here is a solution in Python:
```python
def factorial(n):
    return product(1..=n)
def product(rng):
    result = 1
    for i in rng:
        result *= i
    return result
for i in range(1, 13):
    print(f"factorial({i}) = {factorial(i)}")
```
This program defines two functions: `factorial(n)` which calculates the factorial of a number `n` using the `product(rng)` function, and `product(rng)` which calculates the product of all numbers in a given range `rng`.
The program then loops over the numbers from 1 to 12, calculates the factorial of each number using the `factorial(n)` function, and prints the result.
[ Prompt: 2829.9 t/s | Generation: 148.9 t/s ]
> 
> 
> 
> 
> 
> 
> 
> 
> 
> 
> 
> 
> 
> 
> 
