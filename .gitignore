# Rust build artifacts
**/target/

# compiled binaries
a.out
*.out

# runtime data
*.grug

# models (large, not tracked)
models/

# python
__pycache__/
.ruff_cache/

# secrets / credentials — never commit these
.env
.env.*
*.pem
*.key
atomic-config.toml
