{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "build": {
      "prompt": "Read `context.grug` for project context. Read `SUCKLESS.md` for design constraints. Build: `cd compiler && cargo build`. Do NOT modify README.md."
    }
  }
}
